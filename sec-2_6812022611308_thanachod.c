#include <stdio.h>
int main(){
    int n,i,O,B,A; // ตัวแปรn=จำนวนธุรกรรม,i=ตัวนับลูป,O=ออนไลน์ 15บาท,B=ที่สาขา 40บาท,A=ตู้ATM 20บาท
    float sum = 0; // sum=ผลรวม

    printf("จำนวนธุรกรรม (3-12) : "); // จำนวนธุรกรรม(3-12)
    scanf("%d", &n); // รับค่าn

    printf("ใส่ประเภท %d ประเภท (O,B,A ของแต่ละครั้ง):\n", n); // กรอกประเภท
    for(i = 1; i <= n; i++) { // วนลูปi=1ถึงn
        printf("ประเภทของคนที่ %d: ", i); // กรอกประเภทของครั้งที่เท่าไร

    }
    return 0;
}